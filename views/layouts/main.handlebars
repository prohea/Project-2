<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>DnD Meetup</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
 <div class="flex-column justify-space-around">
    <header class="display-flex justify-space-between align-center p-2 bg-slate-500">
      <a href="/">
        DnD Meetups
      </a>
    </header>
      <nav>
        <!-- Conditionally render login or logout links -->
        {{#if logged_in}}
        <a href="../profile" class="border-2 border-black rounded bg-red-500 text-white login-button">Profile</a> |
        <button class="border-2 border-black rounded bg-red-500 text-white login-button" id="logout">Logout</button>
        {{else}}
        <a href="../login" class="border-2 border-black rounded bg-red-500 text-white login-button">Login</a>
        {{/if}}
      </nav>
    </header>
  <main class="container container-fluid mt-5">
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>
    <footer>
      
    </footer>
  </div>
{{#if logged_in}}
  <script src="/js/logout.js"></script>
{{/if}}
</body>




</html>
